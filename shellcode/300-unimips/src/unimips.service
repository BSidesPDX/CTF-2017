service unimips
{
        disable         = no
        flags           = REUSE
        socket_type     = stream
        wait            = no
        user            = nobody
        server          = /usr/bin/qemu-mipsel-static
        server_args     = /unimips
}
