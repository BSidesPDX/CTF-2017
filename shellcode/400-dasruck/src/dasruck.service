service dasruck
{
        disable         = no
        flags           = REUSE
        socket_type     = stream
        wait            = no
        user            = nobody
        server          = /dasruck
}
